#---
#language: node_js
#node_js:
#  - "4"
#
#sudo: false
#
#cache:
#  directories:
#    - node_modules
#
#before_install:
#  - cd client
#  - npm config set spin false
#  - npm install -g bower
#  - bower --version
#  - npm install phantomjs-prebuilt
#  - phantomjs --version
#
#install:
#  - npm install
#  - bower install
#
#script:
#  - npm test
sudo: required
language: node_js
node_js:
  - "4"
services:
  - mongodb
python:
  - "2.7"
cache:
  directories:
  - node_modules
install:
  - sudo apt-get update
  # We do this conditionally because it saves us some downloading if the
  # version is the same.
  - wget https://repo.continuum.io/miniconda/Miniconda2-latest-Linux-x86_64.sh -O miniconda.sh
  - bash miniconda.sh -b -p $HOME/miniconda
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q conda
  # Useful for debugging any issues with conda
  - conda info -a
  # Replace dep1 dep2 ... with your dependencies
  - conda create -q -n test-environment python="2.7" atlas numpy scipy matplotlib nose dateutil pandas statsmodels
  - source activate test-environment
  - cd reporter-etl
  - python buildDB.py install
after_install:
  - cd ../server
before_script:
  - npm prune
script:
  - npm test
