import { wait } from 'client/helpers/wait';
import { module, test } from 'qunit';

module('Unit | Helper | wait');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = wait([42]);
  assert.ok(result);
});
