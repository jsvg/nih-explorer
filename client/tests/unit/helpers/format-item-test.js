//import { formatItem } from 'client/helpers/format-item';
import { module, test } from 'qunit';

module('Unit | Helper | format item');

// Replace this with your real tests.
test('it works', function(assert) {
  //let result = formatItem([42]);
  assert.ok('result');
});
