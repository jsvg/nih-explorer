{{! search/index}}

{{search.index.search-table
  grants=model
  showGrantModal=(action (pipe (route-action 'setModalGrant') (toggle 'isShowingModal' this)))
  pageButtonClick=(route-action 'paginator')
  paginationData=paginationLinks
}}

{{search.index.search-table-modal
  isShowingModal=isShowingModal
  modalGrant=modalGrant
}}
